apiVersion: apps/v1
kind: Deployment
metadata:
  name: phone-validator-backend
  creationTimestamp: null
  labels:
    app: phone-validator-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: phone-validator-backend
  strategy: 
      type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: phone-validator-backend
    spec:
      containers:
      - image: johnylima/phone-validator-backend
        name: phone-validator-backend
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        ports:
            - name: frontend-port
              containerPort: 8080
              hostPort: 8080

status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: lb-phone-validator-backend
  labels:
    app: lb-phone-validator-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: phone-validator-backend

